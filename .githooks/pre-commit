#!/usr/bin/env bash
set -u
set -o pipefail

if [[ "${SKIP_HOOKS:-}" == "1" ]]; then
    exit 0
fi

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"

echo "pre-commit: running lint gate"
bash scripts/test.sh gate:lint || exit 1
