# Operations module

Owns read-only operational aggregation and Inertia pages for dashboard and operations views. It may import Flickr and Transfer for snapshots; peer modules must not import Operations or place transfer mutation rules here.

See `docs/00-architecture/modules.md`.
