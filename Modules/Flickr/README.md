# Flickr module

Owns connected Flickr user accounts, OAuth, crawl triggers, crawl-run presentation, and token health. App credentials belong to Settings; crawler execution belongs to Crawler.

See `docs/00-architecture/modules.md`.
