# Settings module

Owns operator-editable runtime configuration and OAuth application credentials stored through laravel-config. Connected Flickr and storage user accounts remain owned by their respective modules.

See `docs/00-architecture/modules.md`.
