# Catalog module

Owns browse UI/API for crawler catalog data: photos, photosets, galleries, and favorites. It reads crawler-owned catalog tables through approved repositories and does not run crawl workflows.

See `docs/00-architecture/modules.md`.
