# Spider module

Owns opt-in spider frontier expansion and spider runs. It must remain explicitly enabled through runtime configuration; it does not replace manual crawl workflows.

See `docs/00-architecture/modules.md`.
