# Contacts module

Owns contact directory, annotations, graph, expansion previews, and full-pass UX. It owns HTTP queue entry points for contact/photo download and upload; Transfer owns execution, batches, and jobs.

See `docs/00-architecture/modules.md`.
